const ContractStatusEnum = {
    NEW: 'new',
    IN_PROGRESS: 'in_progress',
    TERMINATED: 'terminated'
}

const ProfileTypesEnum = {
    CLIENT: 'client',
    CONTRACTOR: 'contractor'
}

module.exports = {ContractStatusEnum, ProfileTypesEnum}